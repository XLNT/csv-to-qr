{
  "name": "csv-to-qr",
  "version": "1.0.0",
  "description": "Given a CSV file, convert urls from a header to associated QR codes, named after another header in a specific directory.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Matt Condon <m@cond.in> (http://mat.tc)",
  "license": "Apache-2.0",
  "dependencies": {
    "csv": "^3.1.0",
    "minimist": "^1.2.0",
    "pify": "^4.0.0",
    "shelljs": "^0.8.2"
  }
}
